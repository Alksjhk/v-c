# Vercel 环境变量配置示例
# 在 Vercel Dashboard 中设置这些环境变量

# PostgreSQL 数据库连接字符串 (必须)
POSTGRES_URL=postgresql://username:password@host:port/database

# CORS 允许的域名 (生产环境)
# 多个域名用逗号分隔，Vercel 域名格式: https://your-project.vercel.app
CORS_ORIGIN=https://your-project.vercel.app

# 端口 (Vercel 会自动分配，通常不需要设置)
PORT=3001

# Node.js 环境 (必须设置为 production)
NODE_ENV=production

# 可选：前端 API 基础 URL (如果前端和后端部署在不同域名)
# VITE_API_BASE_URL=https://your-backend.vercel.app
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_KR8UW7nDABRt6rv2_82WVLw8r3lC3pLPTMpWaMZ6gpl5omk"